<UserControl x:Class="ExperimentalControlPlatform.App.Widgets.DeviceControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="ControlPanelRoot">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <ContentPresenter Content="{Binding ConfigurationContent, ElementName=ControlPanelRoot}"
                        DataContext="{Binding DataContext, ElementName=ControlPanelRoot}" />

      <ContentPresenter Grid.Row="1"
                        Content="{Binding ActionsContent, ElementName=ControlPanelRoot}"
                        DataContext="{Binding DataContext, ElementName=ControlPanelRoot}" />
    </Grid>

    <ContentPresenter Grid.Row="2"
                      Margin="0,12,0,0"
                      Content="{Binding DiagnosticsContent, ElementName=ControlPanelRoot}"
                      DataContext="{Binding DataContext, ElementName=ControlPanelRoot}" />
  </Grid>
</UserControl>
